#include "main.h"
/**
 * print_alphabet - prints the alphabet
 *
 * Return: no return
 */
void print_alphabet(void)
{
	char counter;

	for (counter = 'a'; counter <= 'z'; counter++)
	{
	_putchar(counter);
	}

	_putchar('\n');
}	
