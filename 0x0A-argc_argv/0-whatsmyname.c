#include "main.h"
#include <stdio.h>

/**
 * main - prints itname followed by new line
 *@argc: number of the type of agruments
 *@argv: array that poin to the argument
 * Return: 0
*/

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
