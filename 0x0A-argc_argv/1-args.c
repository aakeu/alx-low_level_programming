#include <studio.h>
#include "main.h"

/**
 * main - Entry Point
 * @agrc: an argument
 * @argv: array point to the arguments
 * Return: 0
 */

int main(int agrc, _attribute_((usused)) char *argv[])
{
	int i;

	for (i = 0; i < argvc; i++)
	{
	}
	printf("%d\n", i - 1);
	return (0);
}
